/*import React from "react";
import { useNotification } from 'react-hook-notification';

export default function useNotify(top,message,type) {

    const notification = useNotification();
    if (type ==="success"){
        notification.success({text:message,title:top})
    } else if (type==="error"){
        notification.error({text:message,title:top})
    } else if (type === "warning"){
        notification.warning({text:message,title:top})
    }
};*/

